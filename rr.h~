#include <stdio.h>
#include "assgn1.h"

// list the method declarations that rr.c will have in order to 
// sort and process the processes struct array

void print_test(int quantium_time);

void pass_struct_array(struct Process *p_array); 
